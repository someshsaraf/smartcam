# FastAPI API + MQTT/WebSocket bridge + OpenCV (diagnostics) + LAN discovery
#
# MediaMTX is NOT a PyPI package — it cannot be installed with pip. The controller
# expects a mediamtx binary (PATH, CONTROLLER_MEDIAMTX_BIN, or backend/bin/mediamtx).
# One-time install:
#   cd controller/backend && ./scripts/install_mediamtx.sh
#
fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0
opencv-python>=4.8
numpy>=1.26
zeroconf>=0.130
httpx>=0.27
paho-mqtt>=2.0

# Hailo runtime is installed from Hailo packages on the Pi, not pip.
# Python import expected at runtime: hailo_platform
